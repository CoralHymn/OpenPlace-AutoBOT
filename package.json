{
  "name": "wplace-autobot",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs --dev",
    "build:ext": "node build.mjs --extension",
    "build:ext:dev": "node build.mjs --extension --dev",
    "watch": "node build.mjs --watch",
    "watch:ext": "node build.mjs --extension --watch",
    "lint": "eslint \"src/**/*.{js,jsx}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.5.0",
    "esbuild": "^0.21.5",
    "eslint": "^9.5.0",
    "prettier": "^3.3.3",
    "pako": "^2.1.0"
  }
}